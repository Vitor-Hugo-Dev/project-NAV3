{
  "name": "nav3",
  "version": "1.0.0",
  "description": "Aplicação de cadastro e visualização de dados referentes a pessoas.",
  "main": "index.js",
  "scripts": {
    "compose:up": "(cd app && docker-compose up -d --build)",
    "compose:down": "(cd app && docker-compose down --remove-orphans)"
  },
  "author": "Claudio Cassimiro / Vitor Hugo",
  "devDependencies": {
    "dotenv": "10.0.0",
    "mysql2": "2.3.3",
    "sequelize": "^6.11.0",
    "sequelize-cli": "^6.4.1"
  }
}
